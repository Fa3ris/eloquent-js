{
  "name": "persistent",
  "version": "1.0.0",
  "description": "dummy lib",
  "main": "out/main.js",
  "types": "out/main.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-watch": "tsc --watch",
    "build": "tsc",
    "pack": "npm pack",
    "cp-build": "node build.js",
    "build-and-pack": "npm run build && npm run pack && npm run cp-build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.7.4",
    "@types/node": "^18.0.1"
  },
  "files": [
    "out/"
  ]
}
